# Employee-Manager
An ASP.NET Core MVC application with user logins, roles, employee create-read-update-delete, pdf report generation and sending report in email.

- Account registration, login, change-reset password through email link
- User authorization (Admin can create an employee, add an user to admin role)
- Create-Read-Update-Delete of an employee
- PDF invoice generation and sending it in email from a form
